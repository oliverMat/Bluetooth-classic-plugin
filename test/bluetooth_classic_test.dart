import 'package:flutter_test/flutter_test.dart';
import 'package:bluetooth_classic/bluetooth_classic.dart';
import 'package:bluetooth_classic/bluetooth_classic_platform_interface.dart';
import 'package:bluetooth_classic/bluetooth_classic_method_channel.dart';
import 'package:plugin_platform_interface/plugin_platform_interface.dart';

/*
class MockBluetoothClassicPlatform
    with MockPlatformInterfaceMixin
    implements BluetoothClassicPlatform {
  @override
  Future<bool> enableBluetooth() {
    // TODO: implement enableBluetooth
    throw UnimplementedError();
  }

  @override
  Future<bool> getPlatformVersion() {
    // TODO: implement getPlatformVersion
    throw UnimplementedError();
  }

  @override
  Future<List<String>> listDevices() {
    // TODO: implement listDevices
    throw UnimplementedError();
  }

  */
/*@override
  Future<String?> getPlatformVersion() => Future.value('42');*//*

}
*/

void main() {
  /*final BluetoothClassicPlatform initialPlatform = BluetoothClassicPlatform.instance;

  test('$MethodChannelBluetoothClassic is the default instance', () {
    expect(initialPlatform, isInstanceOf<MethodChannelBluetoothClassic>());
  });

  test('getPlatformVersion', () async {
    BluetoothClassic bluetoothClassicPlugin = BluetoothClassic();
    MockBluetoothClassicPlatform fakePlatform = MockBluetoothClassicPlatform();
    BluetoothClassicPlatform.instance = fakePlatform;

    expect(await bluetoothClassicPlugin.getPlatformVersion(), '42');
  });*/
}
